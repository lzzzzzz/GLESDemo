<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lz.gles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FirstEsActivity" />
        <activity android:name=".activity.SecondESActivity" />
        <activity android:name=".activity.ThirdESActivity" />
        <activity android:name=".activity.FourthESActivity" />
        <activity android:name=".activity.FifthEsActivity" />
        <activity android:name=".activity.SixEsActivity"></activity>
    </application>

</manifest>